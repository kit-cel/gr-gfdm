# auto-generated by grc.converter

id: gfdm_cyclic_prefixer_cc
label: GFDM Cyclic Prefixer
category: '[GFDM]'

parameters:
-   id: cp_length
    label: CP length
    dtype: int
    default: '0'
-   id: cs_length
    label: CS length
    dtype: int
    default: '0'
-   id: ramp_len
    label: Ramp length
    dtype: int
    default: '0'
-   id: block_len
    label: Block length
    dtype: int
    default: '0'
-   id: window_taps
    label: Window taps
    dtype: raw

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.cyclic_prefixer_cc(${block_len}, ${cp_length}, ${cs_length}, ${ramp_len},
        ${window_taps})

file_format: 1
