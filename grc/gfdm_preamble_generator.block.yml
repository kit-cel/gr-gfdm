# auto-generated by grc.converter

id: variable_preamble_generator
label: GFDM Preamble Generator
category: '[GFDM]'
flags:
- show_id

parameters:
-   id: nsubcarrier
    label: Nsubcarrier
    dtype: int
    default: '16'
-   id: filter_alpha
    label: Filter alpha
    dtype: real
    default: '0.35'
-   id: sync_fft_len
    label: Sync FFT Len
    dtype: int
    default: '32'
value: ${ gfdm.preamble_generator(nsubcarrier, filter_alpha, sync_fft_len) }

templates:
    imports: import gfdm
    var_make: self.${id} = ${id} = gfdm.preamble_generator(${nsubcarrier}, ${filter_alpha},
        ${sync_fft_len}).base()

file_format: 1
