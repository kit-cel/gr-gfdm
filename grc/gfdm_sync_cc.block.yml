# auto-generated by grc.converter

id: gfdm_sync_cc
label: GFDM Sync
category: '[GFDM]'

parameters:
-   id: n_subcarriers
    label: '#subcarriers'
    dtype: int
    default: '32'
-   id: cp_length
    label: CP length
    dtype: int
    default: '0'
-   id: frame_len
    label: Frame length
    dtype: int
    default: '1024'
-   id: preamble
    label: Preamble
    dtype: raw
-   id: gfdm_tag_key
    label: GFDM tag key
    dtype: string
    default: '"gfdm_block"'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.sync_cc(${n_subcarriers}, ${cp_length}, ${frame_len}, ${preamble},
        ${gfdm_tag_key})

file_format: 1
