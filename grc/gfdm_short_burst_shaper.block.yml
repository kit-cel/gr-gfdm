# auto-generated by grc.converter

id: gfdm_short_burst_shaper
label: Short Burst Shaper
category: '[GFDM]'

parameters:
-   id: pre_padding
    label: Pre-padding Length
    dtype: int
    default: '128'
-   id: post_padding
    label: Post-padding Length
    dtype: int
    default: '128'
-   id: length_tag_name
    label: Length Tag Name
    dtype: string
    default: '"packet_len"'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.short_burst_shaper(${pre_padding}, ${post_padding}, ${length_tag_name})

file_format: 1
