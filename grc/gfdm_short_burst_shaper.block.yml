# auto-generated by grc.converter

id: gfdm_short_burst_shaper
label: Short Burst Shaper
category: '[GFDM]'

parameters:
-   id: pre_padding
    label: Pre-padding Length
    dtype: int
    default: '128'
-   id: post_padding
    label: Post-padding Length
    dtype: int
    default: '128'
-   id: scale
    label: Scale
    dtype: complex
    default: '1.0+0.0j'
-   id: length_tag_name
    label: Length Tag Name
    dtype: string
    default: '"packet_len"'
-   id: use_timed_commands
    label: Timed commands
    dtype: bool
    default: False
-   id: timing_advance
    label: Timing advance
    dtype: float
    default: 1.0e-3

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: time_tag
    optional: True

outputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: command
    optional: True

templates:
    imports: import gfdm
    make: gfdm.short_burst_shaper(${pre_padding}, ${post_padding}, ${scale}, ${length_tag_name}, ${use_timed_commands}, ${timing_advance})
    callbacks:
    - set_scale(${scale})
    - set_timing_advance(${timing_advance})

file_format: 1
