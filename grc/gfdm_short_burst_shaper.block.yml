# auto-generated by grc.converter

id: gfdm_short_burst_shaper
label: Short Burst Shaper
category: '[GFDM]'

parameters:
-   id: pre_padding
    label: Pre-padding Length
    dtype: int
    default: '128'
-   id: post_padding
    label: Post-padding Length
    dtype: int
    default: '128'
-   id: scale
    label: Scale
    dtype: complex
    default: '1.0+0.0j'
-   id: length_tag_name
    label: Length Tag Name
    dtype: string
    default: '"packet_len"'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.short_burst_shaper(${pre_padding}, ${post_padding}, ${scale}, ${length_tag_name})
    callbacks:
    - set_scale(${scale})

file_format: 1
