# auto-generated by grc.converter

id: gfdm_modulator_cc
label: GFDM Modulator
category: '[GFDM]'

parameters:
-   id: nsubcarrier
    label: Nsubcarrier
    dtype: int
    default: '16'
-   id: ntimeslots
    label: Ntimeslots
    dtype: int
    default: '16'
-   id: filter_alpha
    label: Filter_alpha
    dtype: real
    default: '0.35'
-   id: fft_len
    label: Fft_len
    dtype: int
    default: '256'
-   id: sync_fft_len
    label: Sync_fft_len
    dtype: int
    default: '32'
-   id: len_tag_key
    label: Len_tag_key
    dtype: string
    default: ' "frame_len"'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.modulator_cc(${nsubcarrier}, ${ntimeslots}, ${filter_alpha}, ${fft_len},
        ${sync_fft_len}, ${len_tag_key})

file_format: 1
