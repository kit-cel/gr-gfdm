# auto-generated by grc.converter

id: gfdm_simple_modulator_cc
label: simple GFDM Modulator
category: '[GFDM]'

parameters:
-   id: n_timeslots
    label: M time slots
    dtype: int
-   id: n_subcarriers
    label: K subcarriers
    dtype: int
-   id: overlap
    label: L overlap
    dtype: int
-   id: frequency_taps
    label: Frequency domain taps
    dtype: raw

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.simple_modulator_cc(${n_timeslots}, ${n_subcarriers}, ${overlap}, ${frequency_taps})

file_format: 1
