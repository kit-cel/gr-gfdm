# auto-generated by grc.converter

id: gfdm_resource_demapper_cc
label: GFDM Resource Demapper
category: '[GFDM]'

parameters:
-   id: active_subcarriers
    label: Active subcarriers
    dtype: int
    default: '0'
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '0'
-   id: timeslots
    label: timeslots
    dtype: int
    default: '0'
-   id: subcarrier_map
    label: subcarrier map
    dtype: raw
-   id: per_timeslot
    label: per timeslot
    dtype: bool
    default: 'True'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.resource_demapper_cc(${timeslots}, ${subcarriers}, ${active_subcarriers},
        ${subcarrier_map}, ${per_timeslot})

file_format: 1
