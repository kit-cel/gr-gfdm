# auto-generated by grc.converter

id: gfdm_frame_energy_detector_cc
label: TDD Frame Detector
category: '[GFDM]'

parameters:
-   id: alpha
    label: Alpha
    dtype: float
-   id: average_len
    label: Average Length
    dtype: int
-   id: frame_len
    label: Frame Length
    dtype: int
-   id: backoff_len
    label: Backoff Length
    dtype: int
-   id: tag_key
    label: Tag Key
    dtype: string

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.frame_energy_detector_cc(${alpha}, ${average_len}, ${frame_len}, ${backoff_len},
        ${tag_key})
    callbacks:
    - set_alpha(${alpha})
    - set_backoff_len(${backoff_len})

file_format: 1
