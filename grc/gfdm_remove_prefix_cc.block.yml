# auto-generated by grc.converter

id: gfdm_remove_prefix_cc
label: GFDM Remove Prefix
category: '[GFDM]'

parameters:
-   id: frame_len
    label: Frame length
    dtype: int
-   id: block_len
    label: Block length
    dtype: int
-   id: offset
    label: Offset
    dtype: int
-   id: gfdm_sync_tag_key
    label: Tag key
    dtype: string
    default: '"frame_start"'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.remove_prefix_cc(${frame_len}, ${block_len}, ${offset}, ${gfdm_sync_tag_key})

file_format: 1
