# auto-generated by grc.converter

id: gfdm_advanced_receiver_sb_cc
label: GFDM Advanced Receiver (SB)
category: '[GFDM]'

parameters:
-   id: timeslots
    label: M timeslots
    dtype: int
    default: '16'
-   id: subcarriers
    label: N subcarriers
    dtype: int
    default: '16'
-   id: overlap
    label: L overlap
    dtype: int
    default: '2'
-   id: ic_iter
    label: Ic_iter
    dtype: int
    default: '16'
-   id: frequency_taps
    label: Frequency domain taps
    dtype: raw
-   id: constellation
    label: Constellation
    dtype: raw
-   id: subcarrier_map
    label: subcarrier map
    dtype: raw
-   id: do_phase_compensation
    label: Do phase compensation
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: complex
-   label: eq
    domain: stream
    dtype: complex
    optional: true

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.advanced_receiver_sb_cc(${timeslots}, ${subcarriers}, ${overlap}, ${ic_iter},
        ${frequency_taps}, ${constellation}, ${subcarrier_map}, ${do_phase_compensation})
    callbacks:
    - set_ic(${ic_iter})
    - set_phase_compensation(${do_phase_compensation})

file_format: 1
