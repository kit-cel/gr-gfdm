# auto-generated by grc.converter

id: gfdm_simple_preamble_sync_cc
label: Preamble GFDM Sync
category: '[GFDM]'

parameters:
-   id: frame_len
    label: Frame length
    dtype: int
-   id: subcarriers
    label: Subcarriers
    dtype: int
-   id: cp_len
    label: Cyclic Prefix length
    dtype: int
-   id: preamble
    label: Preamble
    dtype: complex_vector
-   id: in_key
    label: Input Tag Key
    dtype: string
-   id: out_key
    label: Output Tag Key
    dtype: string

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: cfo
    optional: true

templates:
    imports: import gfdm
    make: gfdm.simple_preamble_sync_cc(${frame_len}, ${subcarriers}, ${cp_len}, ${preamble},
        ${in_key}, ${out_key})

file_format: 1
