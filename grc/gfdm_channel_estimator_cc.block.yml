# auto-generated by grc.converter

id: gfdm_channel_estimator_cc
label: GFDM Channel Estimator
category: '[GFDM]'

parameters:
-   id: timeslots
    label: Timeslots
    dtype: int
-   id: fft_len
    label: FFT length
    dtype: int
-   id: active_subcarriers
    label: Active Subcarriers
    dtype: int
-   id: is_dc_free
    label: is DC free
    dtype: bool
-   id: which_estimator
    label: Estimator Type
    dtype: int
    default: '0'
-   id: preamble
    label: Preamble
    dtype: complex_vector

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gfdm
    make: gfdm.channel_estimator_cc(${timeslots}, ${fft_len}, ${active_subcarriers},
        ${is_dc_free}, ${which_estimator}, ${preamble})

file_format: 1
